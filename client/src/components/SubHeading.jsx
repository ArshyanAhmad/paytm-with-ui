import React from "react";

const SubHeading = ({ label }) => {
   return (
      <div>
         <h4 className="text-sm text-center pb-3">{label} </h4>
      </div>
   );
};

export default SubHeading;
